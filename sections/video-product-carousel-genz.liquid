{% comment %}
  Gen Z Video Product Carousel
  Instagram-style video product grid with modern, vibrant styling
{% endcomment %}

<section class="video-product-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title{% if section.settings.use_gradient_heading %} gradient-text{% endif %}">{{ section.settings.title | escape }}</h2>
      {% if section.settings.subheading != blank %}
        <div class="section-subheading{% if section.settings.use_gradient_subheading %} gradient-text{% endif %}">
          {{ section.settings.subheading }}
        </div>
      {% endif %}
      
      {% if section.settings.view_all_text != blank and section.settings.view_all_link != blank %}
        <a href="{{ section.settings.view_all_link }}" class="view-all-link">{{ section.settings.view_all_text }} →</a>
      {% endif %}
    </div>

    <div class="video-product-grid">
      {% for block in section.blocks %}
        {% if block.type == 'video_product' %}
          <div class="video-product" {{ block.shopify_attributes }}>
            <div class="video-container">
              <video class="product-video" autoplay loop muted playsinline poster="{{ block.settings.poster_image | image_url: width: 520 }}">
                <source src="{{ block.settings.video_url }}" type="video/mp4">
                Your browser does not support the video tag.
              </video>
              <div class="video-views">{{ block.settings.view_count | default: '12K' }} Views</div>
              
              {% if block.settings.show_wishlist %}
                <div class="wishlist-icon" data-product-id="{{ block.settings.product_id }}">
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
                    <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
                  </svg>
                </div>
              {% endif %}
            </div>
            <div class="video-product-info">
              <h3 class="video-product-title">{{ block.settings.title }}</h3>
              <div class="video-product-price">{{ block.settings.price }}</div>
              <a href="{{ block.settings.product_url }}" class="buy-now-btn">{{ block.settings.button_text | default: 'Buy Now' }}</a>
            </div>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Video Product Carousel",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "🔥 Trending Now"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Check out our most popular products"
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text Alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "Heading Size",
      "default": 28,
      "min": 10,
      "max": 50,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#ccff00"
    },
    {
      "type": "select",
      "id": "heading_weight",
      "label": "Heading Weight",
      "options": [
        {
          "value": "400",
          "label": "Regular"
        },
        {
          "value": "600",
          "label": "Semi-bold"
        },
        {
          "value": "700",
          "label": "Bold"
        }
      ],
      "default": "600"
    },
    {
      "type": "checkbox",
      "id": "use_gradient_heading",
      "label": "Use Gradient Heading",
      "default": false,
      "info": "When enabled, applies Instagram-style gradient to heading text"
    },
    {
      "type": "range",
      "id": "subheading_size",
      "label": "Subheading Size",
      "default": 16,
      "min": 10,
      "max": 30,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Subheading Color",
      "default": "#bbbbbb"
    },
    {
      "type": "select",
      "id": "subheading_weight",
      "label": "Subheading Weight",
      "options": [
        {
          "value": "400",
          "label": "Regular"
        },
        {
          "value": "600",
          "label": "Semi-bold"
        },
        {
          "value": "700",
          "label": "Bold"
        }
      ],
      "default": "400"
    },
    {
      "type": "checkbox",
      "id": "use_gradient_subheading",
      "label": "Use Gradient Subheading",
      "default": false,
      "info": "When enabled, applies Instagram-style gradient to subheading text"
    },
    {
      "type": "color",
      "id": "gradient_color_1",
      "label": "Gradient Color 1",
      "default": "#ff6bff"
    },
    {
      "type": "color",
      "id": "gradient_color_2",
      "label": "Gradient Color 2",
      "default": "#00f5ff"
    },
    {
      "type": "color",
      "id": "gradient_color_3",
      "label": "Gradient Color 3",
      "default": "#8a2be2"
    },
    {
      "type": "text",
      "id": "view_all_text",
      "label": "View All Text",
      "default": "View All"
    },
    {
      "type": "url",
      "id": "view_all_link",
      "label": "View All Link"
    }
  ],
  "blocks": [
    {
      "type": "video_product",
      "name": "Video Product",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Product Title",
          "default": "Product Name"
        },
        {
          "type": "text",
          "id": "price",
          "label": "Product Price",
          "default": "$29.99"
        },
        {
          "type": "url",
          "id": "video_url",
          "label": "Video URL",
          "info": "Link to MP4 video file"
        },
        {
          "type": "image_picker",
          "id": "poster_image",
          "label": "Poster Image",
          "info": "Shown before video loads"
        },
        {
          "type": "text",
          "id": "view_count",
          "label": "View Count",
          "default": "12K",
          "info": "Displayed as '12K Views'"
        },
        {
          "type": "url",
          "id": "product_url",
          "label": "Product URL"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button Text",
          "default": "Buy Now"
        },
        {
          "type": "checkbox",
          "id": "show_wishlist",
          "label": "Show Wishlist Icon",
          "default": true
        },
        {
          "type": "text",
          "id": "product_id",
          "label": "Product ID",
          "info": "Used for wishlist functionality"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Video Product Carousel",
      "category": "Products",
      "blocks": [
        {
          "type": "video_product"
        },
        {
          "type": "video_product"
        },
        {
          "type": "video_product"
        },
        {
          "type": "video_product"
        }
      ]
    }
  ]
}
{% endschema %}

{% stylesheet %}
.video-product-section {
  padding: 60px 0;
  position: relative;
  overflow: hidden;
}

.section-header {
  text-align: {{ section.settings.text_alignment }};
  margin-bottom: 40px;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: {{ section.settings.text_alignment }};
}

.section-header::after {
  content: '';
  display: block;
  width: 60px;
  height: 3px;
  background: linear-gradient(45deg, {{ section.settings.gradient_color_1 }}, {{ section.settings.gradient_color_2 }}, {{ section.settings.gradient_color_3 }});
  margin: 15px auto 0;
  border-radius: 3px;
  
  {% if section.settings.text_alignment == 'left' %}
    margin-left: 0;
  {% elsif section.settings.text_alignment == 'right' %}
    margin-left: auto;
    margin-right: 0;
  {% endif %}
}

.section-title {
  font-size: {{ section.settings.heading_size }}px;
  color: {{ section.settings.heading_color }};
  margin-bottom: 10px;
  font-weight: {{ section.settings.heading_weight }};
  line-height: 1.2;
}

.section-title.gradient-text {
  background: linear-gradient(to right, {{ section.settings.gradient_color_1 }}, {{ section.settings.gradient_color_2 }}, {{ section.settings.gradient_color_3 }});
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  display: inline-block;
}

.section-subheading {
  font-size: {{ section.settings.subheading_size }}px;
  line-height: 1.5;
  margin-top: 5px;
  margin-bottom: 20px;
  max-width: 800px;
  color: {{ section.settings.subheading_color }};
  font-weight: {{ section.settings.subheading_weight }};
}

.section-header.text-center .section-subheading {
  margin-left: auto;
  margin-right: auto;
}

.section-header.text-right .section-subheading {
  margin-left: auto;
  margin-right: 0;
}

.section-header.text-left .section-subheading {
  margin-left: 0;
  margin-right: auto;
}

.section-subheading.gradient-text {
  background: linear-gradient(to right, {{ section.settings.gradient_color_1 }}, {{ section.settings.gradient_color_2 }}, {{ section.settings.gradient_color_3 }});
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  display: inline-block;
}

.view-all-link {
  color: {{ section.settings.gradient_color_1 }};
  font-weight: 600;
  margin-top: 10px;
  display: inline-block;
  transition: all 0.3s ease;
}

.view-all-link:hover {
  color: {{ section.settings.gradient_color_2 }};
  transform: translateX(5px);
}

.video-product-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 15px;
  max-width: 520px;
  margin: 0 auto;
}

.video-product {
  position: relative;
  border-radius: 10px;
  overflow: hidden;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  background-color: #1a1a1a;
  border: 1px solid #2a2a2a;
}

.video-container {
  position: relative;
  aspect-ratio: 1/1;
}

.product-video {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.video-views {
  position: absolute;
  bottom: 10px;
  right: 10px;
  background-color: rgba(0, 0, 0, 0.7);
  color: var(--white);
  padding: 3px 8px;
  border-radius: 20px;
  font-size: 0.8rem;
}

.wishlist-icon {
  position: absolute;
  top: 10px;
  right: 10px;
  width: 30px;
  height: 30px;
  background-color: rgba(0, 0, 0, 0.5);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  z-index: 2;
  transition: all 0.3s ease;
}

.wishlist-icon:hover {
  background-color: {{ section.settings.gradient_color_1 }};
}

.wishlist-icon svg {
  width: 16px;
  height: 16px;
  fill: none;
  stroke: var(--white);
  stroke-width: 2;
  transition: all 0.3s ease;
}

.wishlist-icon.active svg {
  fill: {{ section.settings.gradient_color_1 }};
  stroke: {{ section.settings.gradient_color_1 }};
}

.video-product-info {
  padding: 15px;
}

.video-product-title {
  font-size: 1.1rem;
  margin-bottom: 5px;
  color: var(--white);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.video-product-price {
  color: {{ section.settings.gradient_color_1 }};
  font-weight: bold;
  margin-bottom: 10px;
}

.buy-now-btn {
  width: 100%;
  padding: 8px;
  background: {{ section.settings.gradient_color_1 }};
  color: var(--black);
  border: none;
  border-radius: 5px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  display: block;
  text-align: center;
}

.buy-now-btn:hover {
  background: linear-gradient(45deg, {{ section.settings.gradient_color_1 }}, {{ section.settings.gradient_color_2 }});
  transform: translateY(-3px);
  box-shadow: 0 5px 15px rgba(255, 107, 255, 0.3);
}

@media (max-width: 767px) {
  .video-product-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 480px) {
  .video-product-grid {
    grid-template-columns: 1fr;
  }
}
{% endstylesheet %}

{% javascript %}
document.addEventListener('DOMContentLoaded', function() {
  // Video autoplay for product videos with IntersectionObserver
  const productVideos = document.querySelectorAll('.product-video');
  
  if ('IntersectionObserver' in window) {
    const videoObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.play();
        } else {
          entry.target.pause();
        }
      });
    }, {
      threshold: 0.5
    });
    
    productVideos.forEach(video => {
      videoObserver.observe(video);
    });
  }
  
  // Wishlist functionality
  const wishlistIcons = document.querySelectorAll('.wishlist-icon');
  
  wishlistIcons.forEach(icon => {
    icon.addEventListener('click', function(e) {
      e.preventDefault();
      this.classList.toggle('active');
      
      // Animation effect
      const heart = this.querySelector('svg');
      heart.style.transform = 'scale(1.3)';
      setTimeout(() => {
        heart.style.transform = 'scale(1)';
      }, 300);
      
      // You could add actual wishlist functionality here
      const productId = this.getAttribute('data-product-id');
      console.log('Toggled wishlist for product ID:', productId);
    });
  });
});
{% endjavascript %}
